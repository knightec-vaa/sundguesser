# Local-only secret material — NEVER commit these.
# The master location pool (plaintext) and encryption key live here.
# The encrypted, safe-to-commit versions live in data/.
secrets/

# Local dev build output (decrypted plaintext games), never committed.
.devdist/
dist/

# Python cache
__pycache__/
*.pyc

# OS / editor noise
.DS_Store
*.swp
node_modules/

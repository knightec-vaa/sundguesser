name: Generate daily game

on:
  schedule:
    # Fixed UTC schedule every day. Using a non-zero minute avoids GitHub
    # Actions' busiest scheduling period at the top of each hour. This is
    # 05:23 Swedish summer time / 04:23 Swedish winter time, leaving ample
    # room for ordinary GitHub scheduling delays before the morning.
    - cron: "23 3 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install cryptography

      - name: Generate today's game
        env:
          LOCATIONS_KEY: ${{ secrets.SUPERSECRETKEY }}
        run: python3 tools/generate_game.py

      - name: Commit and push new game data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "chore: publish daily game $(date -u +%Y-%m-%d)"
            git push
          fi
